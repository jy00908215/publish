#include<stdio.h>
#include<stdlib.h>
typedef struct _node {
	int value;
	int expon;
	struct _node *next;
} node;
typedef struct  _list{
	node* head; 
} *list;
list read();
list read(){
	int cnt,value,expon,i;
	list l=(list)malloc(sizeof(list));
	scanf("%d",&cnt);
	if (0<n){
		for(i=0;cnt>i;i++){
		scanf("%d%d",&value,&expon,);
		node 
		}
	}
	
}
