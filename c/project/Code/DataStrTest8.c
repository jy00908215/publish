#include <stdio.h>
#include <string.h>
typedef struct _TreeLink{
	int * Left;
	int * right;
	int data;
} TreeLink;
typedef struct _LinkList{
	TreeLink * head; 
} LinkList;
int main()
void ReadTree(LinkList * pList);
void BuildTree(LinkList * pList);
void 
{

	return 0; 
}


	int NoteNumber;
	scanf("%d",&NoteNumber);
	int i,TmpData;
	for (i=0;NoteNumber>i;i++){
		char input[5];
		scanf("%s",input);
		if(strcmp(input,"Pop")==0){
		}
		elst{
			scanf ("%d",TmpData);
			
		}
	}
